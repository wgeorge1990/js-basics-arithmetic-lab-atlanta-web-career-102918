/*
Variables defined in the test suite:
- oldID
- currentAge

You should NOT try to redefine any of the above variables.
*/

// Write your code below this line!

//newID Adds oldID which too one billion
let newID = 1000000000 + oldID

//checks integer value of currentAge(integer) and returns true or false.
const ageIsValid = Number.isInteger(currentAge)

//Generates random number between 0 and 20
const randomNumber = (Math.random() * 10) + 1

const randomInteger = Math.floor(randomNumber)

let randomUserID = randomInteger + 1000000000
